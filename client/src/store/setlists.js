const initialState = [
    { userId: 1, setListId: '139b394d', comments: "I love this setlist.", isStarred: true }
]

function reducer(state = initialState, action) {
    return state;
}

export default reducer;